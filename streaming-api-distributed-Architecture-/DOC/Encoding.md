# what we need
* so it is still the same the encoder has to loop and ask spring if there is a job ready
* spring will give it the adress to the new video
* it will encode it and specifgy wher  it has put the destintion
* to spring spring will then work with it 
* and update it that is it

so those are the thing that we will be doign


* one we need to send to the encode  wher to find the staff and what to anme staff
* the encoder need to do checks on the vid, its header and deep check, and sned propor info to 
spring and we will tell spring that the file is delted cause it was not  a vid or is bad formated
* for the feture we can add an aync prosses to send  email to the user
* the vid should't be removed or we should add complex cheks on it on hot to deal with removed staff
* and once the encoding is done and set in the write place we will tell spring where to find those files
* and then the encoder had to delte those files in the end.
